#include <iostream>
#include <math.h>

class Funcoes
{
    public:
        int MDC(int, int);
        int SomaDigitos(unsigned long long int);
        int ConverteBinario(unsigned long long int, int);
        bool VerificarPrimo(int, int);
};
